fn main()->int{
    let x:int = 1>0;
    let a:int = 1 > 0 && 2 > 1 && 3 < 0;
    // 0
    let b:int = 1 < 0 || 2 < 1 || 5 > 4;
    // 1
    a = a > 1;
    if(1>0&&3>2||0>6){
        ret b*10+a;
    }
    ret 1;
}