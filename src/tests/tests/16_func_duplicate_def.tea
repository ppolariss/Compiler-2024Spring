fn foo(a:int, b:int)->int{
    if(a > b || b < a){
        ret b - 1;
    }else{
        ret b + 1;
    }
}
fn foo(a:int, b:int)->int{
    if(a > b || b < a){
        ret b - 1;
    }else{
        ret b + 1;
    }
}

// line 8