fn foo(a:int, b:int)->int{
	ret b - a;
}

fn foo(a:int, b:int)->int{
    ret a + b;
}

fn foo(a:int, b:int, c:int)->int{
	ret b - a + c;
}

fn foo(a:int, b:int)->int{
    ret a + b;
}

fn foo(a:int, b:int)->int;
fn foo(a:int)->int{
}